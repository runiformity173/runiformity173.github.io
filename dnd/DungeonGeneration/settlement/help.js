const helpText = `Select 'Settlement', 'Building', or a specific building type.
"Generate" to generate.
'Settlement' gives random settlement information.
'Building' tells you what the building is, then gives information about it.
Specific building types are the same as building, but the type isn't random.`;