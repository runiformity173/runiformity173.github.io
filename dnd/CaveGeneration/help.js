const helpText = `"Width" and "Height" control the resolution.
"Room Size" controls how big the cavern rooms are.
For a bigger looking cave system, choose a smaller room size.
"Display Only Largest Connected Cave" does exactly what it says when checked.
You can change the cavern ("Floor") color and the "Wall" color.
Press "Reset" on either of the colors to reset them to defaults.
Press "Apply Display Settings" to keep the current cave layout, but change colors or display largest settings.

"Clear" takes you back to the generation settings, and clears the current cave.
"Smooth" makes the cave smoother.`;